/**
 * Role-specific results assembler namespace.
 * Role HTML assemblers register on AIRB.Roles from airb-front.js.
 */
(function () {
	'use strict';
	window.AIRB = window.AIRB || {};
	AIRB.Roles = AIRB.Roles || {};
}());
