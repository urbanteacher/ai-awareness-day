/**
 * Misinformation Detector — interactive article (shortcode [aiad_misinformation_detector])
 */
(function () {
	'use strict';

	var ITEMS = [
		{
			type: 'headline',
			typeLabel: 'Real BBC headline',
			claim:
				'AI deepfake videos of real doctors are spreading health misinformation on social media to sell supplements.',
			sourceLabel: 'BBC News / Full Fact investigation, December 2025',
			context:
				'This headline appeared across major UK news outlets in late 2025 following a Full Fact investigation. A parent shares it with you before a school parents’ evening on digital safety.',
			answer: 'true',
			explanation:
				'Verified true. Full Fact’s investigation uncovered hundreds of deepfake videos using AI-altered footage of real NHS and celebrity doctors to sell unproven supplements — targeting primarily women experiencing menopause. Videos were hosted on TikTok for weeks before removal.',
			verify: [
				{ label: 'Full Fact — AI deepfakes of real doctors investigation', url: 'https://fullfact.org/' },
				{ label: 'BBC News — health misinformation reporting', url: 'https://www.bbc.co.uk/news' },
				{ label: 'British Medical Journal — guidance on identifying health deepfakes', url: 'https://www.bmj.com/' },
			],
			lesson:
				'Just because a video features a real, recognisable doctor does not mean it is genuine. AI can clone faces and voices convincingly. Always verify health claims with the NHS or GOV.UK.',
		},
		{
			type: 'ai',
			typeLabel: 'AI-generated claim',
			claim:
				'"Research shows that students who use AI tools for homework consistently achieve higher grades than those who do not, with a 23% improvement in exam performance across UK secondary schools."',
			sourceLabel: 'Generated by an AI chatbot when asked “does AI help students?”, May 2026',
			context:
				'A well-meaning colleague pastes this into the staff WhatsApp group before your school’s AI policy meeting. Several teachers have already replied “wow, that’s significant.”',
			answer: 'false',
			explanation:
				'No such study exists. The specific “23% improvement” statistic is an AI hallucination — fabricated but stated with confidence. Real research on AI and student outcomes is mixed and actively contested.',
			verify: [
				{ label: 'Google Scholar — search for the claimed study', url: 'https://scholar.google.com/' },
				{ label: 'Education Endowment Foundation research database', url: 'https://educationendowmentfoundation.org.uk/' },
				{ label: 'BBC Reality Check', url: 'https://www.bbc.co.uk/news/reality_check' },
			],
			lesson:
				'AI confidently invents statistics. A specific-sounding number like “23%” makes a false claim far more convincing. Always search for the named study. If you cannot find it, it likely does not exist.',
		},
		{
			type: 'viral',
			typeLabel: 'Viral social media post',
			claim:
				'"BREAKING: The UK government has banned ChatGPT in all state schools from September 2025. Teachers caught using it face disciplinary action."',
			sourceLabel: 'Screenshot shared widely in UK teacher Facebook groups, June 2025',
			context:
				'This screenshot was shared thousands of times in UK teacher Facebook groups over a single weekend. Several teachers in your school are alarmed.',
			answer: 'false',
			explanation:
				'The UK government has not banned ChatGPT or any AI tool in state schools. The DfE published guidance encouraging responsible use rather than a blanket ban. This post is misinformation — likely from misreading a local school policy.',
			verify: [
				{ label: 'DfE — Generative AI in education guidance', url: 'https://www.gov.uk/government/publications' },
				{ label: 'BBC News — search for a government ban announcement', url: 'https://www.bbc.co.uk/news' },
				{ label: 'TES — education policy reporting', url: 'https://www.tes.com/' },
			],
			lesson:
				'Viral posts that confirm existing fears spread fastest. “BREAKING” and “disciplinary action” are fear triggers. Always check GOV.UK directly before acting on policy claims shared on social media.',
		},
		{
			type: 'headline',
			typeLabel: 'Real headline — context matters',
			claim:
				'"Almost 7,000 UK students caught cheating with AI in 2023–24 — and experts say this is just the tip of the iceberg."',
			sourceLabel: 'The Guardian, July 2025, based on Freedom of Information requests',
			context:
				'This headline goes viral among teachers. A colleague says “this proves we need to ban AI in schools entirely.” But does the number mean what people think it means?',
			answer: 'mixed',
			explanation:
				'The headline is accurate — The Guardian verified 7,000 proven AI cheating cases via Freedom of Information requests. However, this represents about 5.1 cases per 1,000 students. Context missing from most shares: traditional plagiarism fell in the same period, and most students use AI as a legitimate study tool.',
			verify: [
				{ label: 'The Guardian — original FOI investigation', url: 'https://www.theguardian.com/' },
				{ label: 'TES — “The damaging myth that GCSE students are AI cheaters”', url: 'https://www.tes.com/' },
				{ label: 'Higher Education Policy Institute', url: 'https://www.hepi.ac.uk/' },
			],
			lesson:
				'A true headline can still mislead. Always ask: what is the denominator? 7,000 sounds alarming. A small percentage of students sounds very different.',
		},
		{
			type: 'ai',
			typeLabel: 'AI-generated legal guidance',
			claim:
				'"The UK’s Online Safety Act 2023 makes it illegal for teachers to share any AI-generated content with students without parental consent. Schools must register with Ofcom before using any AI tools in the classroom."',
			sourceLabel: 'Copied from a ChatGPT response shared in a school CPD WhatsApp group, January 2026',
			context:
				'A colleague shares this as “guidance” before your school’s AI policy meeting. The headteacher has already asked whether you need to contact Ofcom.',
			answer: 'false',
			explanation:
				'The Online Safety Act 2023 contains no such provision. Schools do not register with Ofcom for AI tool use. This is a confident AI hallucination combining real legislation with invented requirements.',
			verify: [
				{ label: 'Online Safety Act 2023 — legislation.gov.uk', url: 'https://www.legislation.gov.uk/' },
				{ label: 'Ofcom — official site', url: 'https://www.ofcom.org.uk/' },
				{ label: 'DfE — AI in schools guidance', url: 'https://www.gov.uk/' },
			],
			lesson:
				'AI confidently invents legal requirements that sound plausible. Any claim about law, Ofcom, or DfE policy must be verified on official GOV.UK sources — never trust a chatbot for legal guidance.',
		},
		{
			type: 'headline',
			typeLabel: 'Real research report',
			claim:
				'"Stanford AI model predicts 130 different diseases from a single night’s sleep data."',
			sourceLabel: 'Nature Medicine journal, reported by BBC News, January 2026',
			context:
				'A science teacher shares this in the staff room as evidence AI is “taking over medicine” and “will replace doctors within five years.”',
			answer: 'mixed',
			explanation:
				'The study is real and peer-reviewed in Nature Medicine. However, “predicts” is doing heavy lifting — the model identifies statistical correlations, not diagnoses. The leap to “replacing doctors in five years” happened in the staff room, not in the study.',
			verify: [
				{ label: 'Nature Medicine', url: 'https://www.nature.com/naturemedicine' },
				{ label: 'BBC News — original report', url: 'https://www.bbc.co.uk/news' },
				{ label: 'NHS — clinical AI guidance', url: 'https://www.nhs.uk/' },
			],
			lesson:
				'Real studies reported accurately can still be dramatically overstated when shared. Always check what the original researchers actually claimed — not what the headline or your colleague said about it.',
		},
	];

	var VERDICTS = [
		{ key: 'true', label: 'Likely true' },
		{ key: 'false', label: 'Likely false' },
		{ key: 'mixed', label: 'Needs context' },
	];

	var HABITS = [
		'Pause on urgency — “share before deleted” is a red flag.',
		'Name the source type: newsroom, AI output, or anonymous viral post.',
		'Log where you would check before you judge.',
		'Separate the number or headline from the story someone wants you to believe.',
		'Teach students the same habit: verify, then verdict.',
	];

	var INSIGHTS = [
		{
			title: 'AI hallucinations',
			text: 'Chatbots invent studies, laws, and statistics. Numbers without citations are not evidence.',
		},
		{
			title: 'Real headlines, stretched meaning',
			text: 'BBC and Guardian stories can be real while captions or WhatsApp forwards add false certainty.',
		},
		{
			title: 'Viral posts mimic news',
			text: 'WhatsApp chains borrow the tone of journalism without links, bylines, or accountability.',
		},
	];

	var state = {
		index: 0,
		points: 0,
		correct: 0,
		sourcesChecked: 0,
		started: false,
	};

	function el(id) {
		return document.getElementById(id);
	}

	function countFilledSources() {
		var n = 0;
		for (var i = 1; i <= 3; i++) {
			var inp = el('aiad-mi-src-' + i);
			if (inp && inp.value.trim() !== '') {
				n++;
			}
		}
		return n;
	}

	function badgeClass(answer) {
		if (answer === 'true') {
			return 'mi-reveal--true';
		}
		if (answer === 'false') {
			return 'mi-reveal--false';
		}
		return 'mi-reveal--mixed';
	}

	function answerLabel(key) {
		if (key === 'true') {
			return 'Likely true';
		}
		if (key === 'false') {
			return 'Likely false';
		}
		return 'Needs context';
	}

	function confidenceBadge(points) {
		if (points >= 66) {
			return { label: 'Media literacy expert', cls: 'mi-conf--expert' };
		}
		if (points >= 46) {
			return { label: 'Discerning reader', cls: 'mi-conf--strong' };
		}
		if (points >= 26) {
			return { label: 'Getting there', cls: 'mi-conf--mid' };
		}
		return { label: 'Needs practice', cls: 'mi-conf--low' };
	}

	function renderProgress() {
		var bar = el('aiad-mi-progress-bar');
		var lbl = el('aiad-mi-progress-lbl');
		if (!bar) {
			return;
		}
		var pct = ((state.index + 1) / ITEMS.length) * 100;
		bar.style.width = pct + '%';
		if (lbl) {
			lbl.textContent = 'Item ' + (state.index + 1) + ' of ' + ITEMS.length;
		}
	}

	function renderItem() {
		var item = ITEMS[state.index];
		var pill = el('aiad-mi-type-pill');
		var claim = el('aiad-mi-claim');
		var source = el('aiad-mi-source');
		var reveal = el('aiad-mi-reveal');
		var nextBtn = el('aiad-mi-next');
		var verdictWrap = el('aiad-mi-verdicts');

		if (pill) {
			pill.textContent = item.typeLabel;
			pill.className = 'mi-type-pill mi-type-pill--' + item.type;
		}
		if (claim) {
			claim.textContent = item.claim;
		}
		if (source) {
			source.textContent = item.sourceLabel;
		}

		var contextEl = el('aiad-mi-context');
		if (contextEl) {
			if (item.context) {
				contextEl.hidden = false;
				contextEl.innerHTML = '<strong>Before you decide:</strong> ' + item.context;
			} else {
				contextEl.hidden = true;
				contextEl.textContent = '';
			}
		}

		var hintEl = el('aiad-mi-sources-hint');
		if (hintEl) {
			hintEl.textContent =
				'Name at least one before choosing a verdict (two or more earns bonus points).';
			hintEl.style.color = '';
		}

		for (var i = 1; i <= 3; i++) {
			var inp = el('aiad-mi-src-' + i);
			if (inp) {
				inp.value = '';
				inp.disabled = false;
			}
		}

		if (reveal) {
			reveal.hidden = true;
			reveal.className = 'mi-reveal';
		}
		if (nextBtn) {
			nextBtn.hidden = true;
		}
		if (verdictWrap) {
			var btns = verdictWrap.querySelectorAll('.mi-verdict-btn');
			btns.forEach(function (b) {
				b.disabled = false;
				b.classList.remove('mi-verdict-btn--picked');
			});
		}

		renderProgress();
	}

	function showReveal(item, picked) {
		var reveal = el('aiad-mi-reveal');
		var nextBtn = el('aiad-mi-next');
		if (!reveal) {
			return;
		}

		var correct = picked === item.answer;
		if (correct) {
			state.correct++;
			state.points += 10;
		}

		var filled = countFilledSources();
		if (filled >= 2) {
			state.points += 5;
			state.sourcesChecked++;
		}

		reveal.hidden = false;
		reveal.className = 'mi-reveal ' + badgeClass(item.answer);

		var title = el('aiad-mi-reveal-title');
		if (title) {
			title.textContent = 'Verdict: ' + answerLabel(item.answer);
		}

		var expl = el('aiad-mi-reveal-expl');
		if (expl) {
			expl.textContent = item.explanation;
		}

		var list = el('aiad-mi-reveal-sources');
		if (list) {
			list.innerHTML = '';
			item.verify.forEach(function (s) {
				var li = document.createElement('li');
				if (s && typeof s === 'object' && s.url) {
					var link = document.createElement('a');
					link.href = s.url;
					link.target = '_blank';
					link.rel = 'noopener noreferrer';
					link.textContent = s.label || s.url;
					li.appendChild(link);
				} else {
					li.textContent = typeof s === 'string' ? s : String(s.label || '');
				}
				list.appendChild(li);
			});
		}

		var lesson = el('aiad-mi-reveal-lesson');
		if (lesson) {
			lesson.textContent = item.lesson;
		}

		var fb = el('aiad-mi-reveal-fb');
		if (fb) {
			fb.textContent = correct
				? '✓ Your verdict matched. +' + (10 + (filled >= 2 ? 5 : 0)) + ' points.'
				: 'Not quite — the best verdict was “' +
				  answerLabel(item.answer) +
				  '”. +' +
				  (filled >= 2 ? 5 : 0) +
				  ' points for checking sources.';
		}

		for (var i = 1; i <= 3; i++) {
			var inp = el('aiad-mi-src-' + i);
			if (inp) {
				inp.disabled = true;
			}
		}

		if (nextBtn) {
			nextBtn.hidden = false;
			nextBtn.textContent =
				state.index < ITEMS.length - 1 ? 'Next item →' : 'See your results';
		}
	}

	function renderResults() {
		var quiz = el('aiad-mi-quiz');
		var results = el('aiad-mi-results');
		if (quiz) {
			quiz.hidden = true;
		}
		if (results) {
			results.hidden = false;
		}

		var conf = confidenceBadge(state.points);
		var badge = el('aiad-mi-conf-badge');
		if (badge) {
			badge.textContent = conf.label;
			badge.className = 'mi-conf-badge ' + conf.cls;
		}

		var pts = el('aiad-mi-stat-points');
		if (pts) {
			pts.textContent = String(state.points);
		}
		var cor = el('aiad-mi-stat-correct');
		if (cor) {
			cor.textContent = state.correct + ' / ' + ITEMS.length;
		}
		var chk = el('aiad-mi-stat-sources');
		if (chk) {
			chk.textContent = String(state.sourcesChecked);
		}
	}

	function onVerdict(key) {
		if (!state.started) {
			return;
		}
		if (countFilledSources() < 1) {
			var hint = el('aiad-mi-sources-hint');
			if (hint) {
				hint.textContent = 'Please name at least one source you would check before choosing a verdict.';
				hint.style.color = '#d85a30';
			}
			var first = el('aiad-mi-src-1');
			if (first) {
				first.focus();
			}
			return;
		}
		var item = ITEMS[state.index];
		var verdictWrap = el('aiad-mi-verdicts');
		if (verdictWrap) {
			verdictWrap.querySelectorAll('.mi-verdict-btn').forEach(function (b) {
				b.disabled = true;
				b.classList.toggle('mi-verdict-btn--picked', b.getAttribute('data-verdict') === key);
			});
		}
		showReveal(item, key);
	}

	function onNext() {
		if (state.index < ITEMS.length - 1) {
			state.index++;
			renderItem();
			return;
		}
		renderResults();
	}

	function start() {
		state.index = 0;
		state.points = 0;
		state.correct = 0;
		state.sourcesChecked = 0;
		state.started = true;

		var intro = el('aiad-mi-intro');
		var quiz = el('aiad-mi-quiz');
		var results = el('aiad-mi-results');
		if (intro) {
			intro.hidden = true;
		}
		if (quiz) {
			quiz.hidden = false;
		}
		if (results) {
			results.hidden = true;
		}
		renderItem();
	}

	function bind() {
		var root = el('aiad-misinfo');
		if (!root || root.getAttribute('data-mi-bound') === '1') {
			return;
		}
		root.setAttribute('data-mi-bound', '1');

		var startBtn = el('aiad-mi-start');
		if (startBtn) {
			startBtn.addEventListener('click', start);
		}

		var verdictWrap = el('aiad-mi-verdicts');
		if (verdictWrap) {
			verdictWrap.querySelectorAll('.mi-verdict-btn').forEach(function (btn) {
				btn.addEventListener('click', function () {
					onVerdict(btn.getAttribute('data-verdict'));
				});
			});
		}

		var nextBtn = el('aiad-mi-next');
		if (nextBtn) {
			nextBtn.addEventListener('click', onNext);
		}

		var retry = el('aiad-mi-retry');
		if (retry) {
			retry.addEventListener('click', function () {
				state.started = false;
				var intro = el('aiad-mi-intro');
				var quiz = el('aiad-mi-quiz');
				var results = el('aiad-mi-results');
				if (results) {
					results.hidden = true;
				}
				if (quiz) {
					quiz.hidden = true;
				}
				if (intro && intro.hidden) {
					start();
				} else if (intro) {
					intro.hidden = false;
				}
			});
		}
	}

	function init() {
		bind();
		var intro = el('aiad-mi-intro');
		if (intro && intro.hidden) {
			start();
		}
	}

	if (document.readyState === 'loading') {
		document.addEventListener('DOMContentLoaded', init);
	} else {
		init();
	}
})();
