# AiAd27 review pages

Static copies of the SlideForge design review, hosted from the theme so the
public site can link them next to Press Release / Asset Pack.

| File | Role |
|---|---|
| `preview.html` | All 35 student slides, live-rendered |
| `style.html` | Tokens, swatches, contrast tables, marks |

Paths are rewritten to reuse `../brand/aiad27` and `../fonts/aiad27`. Refresh
from SlideForge with the same path patches when the decks or guide change.
